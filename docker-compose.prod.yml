services:
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: filex-api
    restart: unless-stopped
    env_file:
      - /srv/docker/secrets/filex.env
    networks:
      - internal
      - proxy

  worker-expiry:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: filex-worker-expiry
    restart: unless-stopped
    command: ["--worker=expiry"]
    env_file:
      - /srv/docker/secrets/filex.env
    networks:
      - internal

  worker-multipart:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: filex-worker-multipart
    restart: unless-stopped
    command: ["--worker=multipart"]
    env_file:
      - /srv/docker/secrets/filex.env
    networks:
      - internal

  worker-gc:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: filex-worker-gc
    restart: unless-stopped
    command: ["--worker=gc"]
    env_file:
      - /srv/docker/secrets/filex.env
    networks:
      - internal

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=https://filex-api.pranavdhawale.in
    container_name: filex-client
    restart: unless-stopped
    networks:
      - proxy

networks:
  internal:
    external: true
  proxy:
    external: true
